[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && pip install -r requirements-prod.txt && python run.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
